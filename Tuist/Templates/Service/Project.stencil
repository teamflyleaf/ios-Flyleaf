//
//  Project.swift
//  {{ name }}
//
//  Created by 여성일 on {{ "now" }}.
//

import ProjectDescription
import ProjectDescriptionHelpers

let project = Project(
  name: "{{ name }}",
  targets: [
    .serviceInterface(.{{ case }}),
    .serviceImplementation(.{{ case }}),
    .serviceTesting(.{{ case }}),
    .serviceTests(.{{ case }})
  ]
)
